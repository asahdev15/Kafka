server:
  port: 8081

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

kafka:
  bootstrap-servers: localhost:9092
  consumer:
    group-id: cg-numbers-sum
  topic:
    product: products
    numbers: numbers
    numbersSum: numbers-sum
